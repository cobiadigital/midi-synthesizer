{
  "name": "midi-synthesizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based monophonic subtractive synthesizer with MIDI input",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/webmidi": "^2.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
