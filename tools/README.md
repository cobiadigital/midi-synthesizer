# tools

Optional Python scripts for offline DSP analysis (spectrum plots, filter
response curves). Nothing here runs in the browser.

Setup, when the first script lands:

```bash
cd tools
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
```
